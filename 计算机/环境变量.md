---
tags:

created: 2026-07-23
---

# 环境变量

## 是什么
环境变量 PATH 是一系列路径的集合，各个路径之间使用冒号 : 隔离开。

比如：
/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin

意味着当我在输入一个命令时，系统便会在 /usr/local/bin 、 /usr/bin: 、 /bin: 、 /usr/sbin 、 /sbin 这些目录下依次寻找是否存在我们输入的那个命令，如果寻找成功，则执行该目录下对应的可执行文件。