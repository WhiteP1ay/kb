---
tags:
  - CSS
  - 布局
created: "2026-07-02"
---

# CSS 包含块

## 是什么

包含块是元素尺寸和位置百分比计算的参考系。不是指直接的父元素，而是根据定位方式决定的祖先元素。

## 关键点

- 静态定位/relative：包含块是最近的块级祖先的内容区
- absolute：包含块是最近的非 static 定位祖先的 padding 区
- fixed：包含块是视口
- `width: 50%` → 相对于包含块宽度的 50%
- `top: 50%` → 相对于包含块高度的 50%
