---
tags:
  - 面试
  - CSS
created: "2026-07-02"
---

# CSS :has() 选择器（父选择器）

## 是什么

`:has()` 允许根据子元素的状态来选择父元素，这就是「父选择器」——过去只能靠 JS 实现，是 CSS 选择器领域的重大突破。

## 关键点

- `.parent:has(.child:hover)` → 子元素悬浮时父元素变色
- 能显著减少 JS 代码量，把交互逻辑留在 CSS 层
- 浏览器兼容性：2023 年后主流浏览器均已支持

## 相关笔记

